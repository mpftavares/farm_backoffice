<?php

verifyAccess();

// $search = isset($_GET['search']) ? $_GET['search'] : 'null';

// $sales = getAllServices($search);

// render('services/list', [
//     'services' => $services
// ]);
